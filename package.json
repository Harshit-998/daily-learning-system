{
  "name": "zero-cost-daily-learning-system",
  "version": "1.0.0",
  "description": "A zero-cost daily System Design + DSA learning automation using Gemini, Gmail, Telegram, and GitHub Actions.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "daily": "node dist/index.js daily",
    "dry-run": "node dist/index.js daily --dry-run",
    "validate": "node dist/index.js validate",
    "preview": "node dist/index.js preview",
    "test": "npm run build && npm run validate && npm run preview"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
